namespace ProjectTheta.Rival
{
    public enum OpponentState
    {
        Idle,
        Search,
        Approach,
        Claiming,
        Contest,
        Stunned
    }

    public enum OpponentTargetMode
    {
        None,
        NeutralClaim,
        Contest
    }
}
